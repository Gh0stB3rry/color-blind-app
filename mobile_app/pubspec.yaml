name: mobile_app
description: A new Flutter project.

publish_to: 'none'

version: 0.0.1+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  geolocator: ^5.1.5
  google_maps_flutter: ^2.2.4
  animated_text_kit: ^4.2.2
  google_fonts: ^2.3.2
  image_picker:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
  - lib/assets/map_style.json